// MODULES

// * common js, every file in module by default;
// * modules - Encapsulated code (only share minimum files)

const { Jordan, Lara } = require("./names");
const { sayHi } = require("./utils");
require("./mindgrenade");

// sayHi("Martin");
// sayHi(Jordan);
// sayHi(Lara);
